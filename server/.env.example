# ProJectra Server – Environment Variables (copy to server/.env)

# Server
PORT=4000
NODE_ENV=development

# MongoDB (Atlas recommended)
# Example Atlas SRV: mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/projectra?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/projectra?retryWrites=true&w=majority
# Optional override if your URI does not include db name
MONGODB_DB_NAME=projectra

# JWT auth
JWT_SECRET=change_me_dev_secret
JWT_EXPIRES_IN=8h
JWT_REFRESH_SECRET=change_me_dev_secret_refresh
JWT_REFRESH_EXPIRES_IN=7d

# CORS – list of allowed origins (comma separated)
# Include your Vite dev server (http://localhost:5173) and any other frontend origins.
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Seed Admin defaults (used by: npm run seed:admin)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin1234!
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User

# AI Assistant (Google Gemini)
# Obtain API key from Google AI Studio: https://aistudio.google.com/
GEMINI_API_KEY=
AI_ASSISTANT_ENABLED=true
AI_RATE_LIMIT_PER_HOUR=100
AI_CONTEXT_MEMORY_LIMIT=10
